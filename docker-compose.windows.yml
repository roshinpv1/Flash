#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.nyxo
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: nousresearch/nyxo-agent:latest
    container_name: nyxo
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.nyxo:/opt/data
    environment:
      - NYXO_UID=10000
      - NYXO_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: nousresearch/nyxo-agent:latest
    container_name: nyxo-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.nyxo:/opt/data
    environment:
      - NYXO_UID=10000
      - NYXO_GID=10000
      - NYXO_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
